#pragma once

// Copy this file to "secrets.h" (in the same folder) and fill in your own
// values. secrets.h is gitignored, so it will never be committed.

#define WIFI_SSID "your_wifi_ssid"
#define WIFI_PASSWORD "your_wifi_password"

// Hostname or IP address of the hub ESP32 (the "Хол" device running
// esp32_bme280_dashboard). Currently unused by the node itself (the node
// never contacts the hub on its own - the hub always polls the node's
// /measure endpoint instead), but kept here in case a future version
// needs it again.
#define HUB_ADDRESS "esp32-home.local"

// Unique id for this node - must match an entry the hub knows about
// (see DEVICE_BEDROOM in the hub's firmware).
#define DEVICE_ID "bedroom"
#define DEVICE_NAME "Спалня"

// Optional: override the BME280 I2C pins if your board's defaults don't
// match your wiring (e.g. some ESP32-C3 boards). Uncomment and adjust if
// the node prints "BME280 not found" at boot.
// #define NODE_SDA_PIN 6
// #define NODE_SCL_PIN 7
